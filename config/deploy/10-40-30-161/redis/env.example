# =========================
# Redis — environment variables (example)
# =========================

SERVICE_NAME=redis

# Host/container port mapping
HOST_REDIS_PORT=<REDIS_PORT>                       # Host port to expose Redis
CONTAINER_REDIS_PORT=6379                          # Redis internal port

# Data persistence (optional)
REDIS_DATA_DIR=/opt/data/redis                    # Host directory for Redis data

# Optional password (uncomment to set AUTH)
# REDIS_PASSWORD=strong-password


