pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

// Use explicit dependency versions instead of version catalog for independence

rootProject.name = 'user-rest-spring-service'